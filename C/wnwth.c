#include <stdio.h>
int main() {
    int a;
    double b;
    char c;
    printf("int 형의 변수의 주소 : %p\n", &a);
    printf("double형의 변수의 주소 : %p\n", &b);
    printf("char형의 변수의 주소 : %p\n", &c);
    return 0;
}